import React from 'react';
import DeviceLayout from 'layouts/TabletScreenLayout';

const Idle = () => {
  return (
    <DeviceLayout>
        <header>
          <img alt="idle-screen" />
        </header>
        <main>
        </main>
    </DeviceLayout>
  );
};

export default Idle;
